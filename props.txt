


constraint, control signals,(not interested in the pmp control signals) (maybe later try symbolic pmp from johannes)
use same traking method as tlb and shared tlb

more about what goes in an out globaly from the module
we dont care about where the data comes from (ptw or shared tlb or private tlbs)


if there is a hit or a valid fetch or dtlb hit, we need to check that translation is right

LIVENESS!!! wie viel clk cycles,  ptw?? plus cache
32 cycles?


black boxing ptw? only for liveness (model ptw abstractly) (constrain the ptw to 8 clk cycles for example)
u can black blox modules or instances in ones spin in our case , black box the ptw instance (between elaboration and compilation) 
u can do it in the gui (options black box) or in tcl script
set compile option - black obox inst

essentilly what it does, it ignores everything in this instance, outputs can be anything, and inputs to this instance do nth essentially

we verified liveness for it speratley (the ptw) so we constrain it here in the MMU scoreboard.

Data integrity (translation in case of hit)
COnstrained livenss

afterwards, test exceptions (for first step, constrain no errors, or exceptions)



Liveness.....
wrapping up the bug

documentation nfor the MMU

the git hub links to the presentation
data inttegrity trace for the waveformfor the bug eport